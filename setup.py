from setuptools import setup, find_packages

setup(
    name = "RBDataHelper",
    version = "1.0",
    description = "",
    long_description = "",
    license = "",

    url = "",
    author = "",
    author_email = "",

    packages = find_packages(),
    include_package_data = True,
    platforms = "any",
    install_requires = [],
    scripts = [],
)